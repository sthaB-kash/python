# find largest number and convert that in lbs (assuming largest num is in kg)
import converter
from max_min import largest
# print(converter.kgs_to_lbs(70))
n = [23, 56, 5, 27, 12]
print(converter.kgs_to_lbs(largest(n)))
